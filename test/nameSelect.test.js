import { drawNames } from '../nameSelect';
import { names1, names2 } from './data.json';

const assert = require('assert');

describe('drawNames', () => {
    describe('logs when name not found', () => {

    });
});